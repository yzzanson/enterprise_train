package com.enterprise.base.config;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.core.io.support.PropertiesLoaderUtils;import java.io.IOException;import java.util.Properties;/** * auther pine * date 18/01/01 * time 上午9:52 */public class Configure {    private static Logger logger = LoggerFactory.getLogger(Configure.class);    //配置文件的基础路径    private static String base_path = null;    private static Properties selfProperties = null;    private static void initProperties() {        try {            if (selfProperties == null) {                selfProperties = PropertiesLoaderUtils.loadAllProperties(base_path + "selfCommonConfig.properties");            }        } catch (IOException e) {            logger.error("加载属性文件失败!", e);        }    }    /**     * ================================== selfApi ================     */    /**     * ==================================getter / setter 方法================     */    public void setBase_path(String base_path) {        this.base_path = base_path;    }    public String getBase_path() {        return base_path;    }}